import React from 'react';
import '../styles/Dashboard.scss';

const Dashboard = () => {
  return (
    <section id="dashboard" className="dashboard">
      <h2>Dashboard</h2>
      <p>Resumen de la sucursal</p>
      {/* Agrega más contenido aquí según los requerimientos */}
    </section>
  );
};

export default Dashboard;
